# Python
.venv/
__pycache__/
*.pyc
*.pyo

# Django
db.sqlite3
media/
staticfiles/
*.log

# Data sampel (SENSITIF — jangan commit)
/samples/

# Env & OS
.env
.DS_Store

# Artefak alat bantu lokal. WAJIB di-ignore, bukan sekadar kerapian repo:
# `railway up` memakai .gitignore untuk menentukan apa yang diunggah, jadi
# tanpa baris ini graphify-out/ (8+ MB) ikut terkirim ke kontainer produksi.
.gstack/
graphify-out/
